# --- PyTorch CPU (index officiel CPU) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1

# --- Web API ---
fastapi==0.115.4
uvicorn[standard]==0.30.6
gunicorn==21.2.0
python-multipart==0.0.9     # nécessaires pour UploadFile/Form

# --- AWS S3 ---
boto3==1.34.162

# --- NLP / Transformers ---
transformers==4.42.4
sentencepiece>=0.1.99        # requis pour mBART, MarianMT (Helsinki-NLP)
sacremoses>=0.0.53           # requis par MarianMT pour la tokenisation
# tokenizers est installé via transformers, inutile de lister séparément

# --- PDF / Utils ---
pypdf==4.2.0
itsdangerous>=2.1.2

langdetect==1.0.9
Jinja2==3.1.4                # utile si plus tard tu utilises des templates Jinja

# --- (optionnel) utiles mais non indispensables ---
# accelerate==0.33.0         # utile si tu veux gérer CPU/GPU facilement
# numpy>=1.26,<3             # installé via torch/transformers en transitive
